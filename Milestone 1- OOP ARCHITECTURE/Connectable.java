//Interface only defines what a function who implements this interface should do !
//this interface connectable says the class who implements this interface should have two functions implemented

// A class can have multiple interfaces
public interface Connectable {


    void turnoff();
    void turnon();

}
